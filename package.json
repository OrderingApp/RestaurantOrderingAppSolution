{
  "scripts": {
    "prepare": "husky",
    "lint:frontend": "npm run lint --prefix frontend",
    "vitest:frontend": "npm run test --prefix frontend",
    "cypress:frontend": "npm run cypress --prefix frontend",
    "test:frontend": "npm run test --prefix frontend",
    "test:frontend:run": "npm run test:run --prefix frontend",
    "check:frontend": "npm run lint:frontend && npm run vitest:frontend"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
