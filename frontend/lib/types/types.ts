export default interface BasicStyles {
    variants: {
        primary: string;
        secondary: string;
        tertiary: string;
        success: string;
        danger: string;
    };
    sizes: {
        sm: string;
        md: string;
        lg: string;
    };
}
